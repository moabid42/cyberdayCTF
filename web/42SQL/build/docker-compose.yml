version: '3'
services:
  scl:
    build: .
    ports:
      - "3004:3004"
    privileged: false
    restart: unless-stopped
    deploy:
      resources:
        limits:
          cpus: '1'
          memory: 2000M