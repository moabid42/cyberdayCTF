version: '4.0'
services:
  llfi:
    build: .
    ports:
      - "4002:3001"
    privileged: false
    restart: unless-stopped
    deploy:
      resources:
        limits:
          cpus: '1'
          memory: 2000M