version: '3'
services:
  llfi:
    build: .
    ports:
      - "3001:3001"
    privileged: false
    restart: unless-stopped
    deploy:
      resources:
        limits:
          cpus: '1'
          memory: 2000M